#attemp to commit something

#Basically add in the folder, than commit using git tab on R studio. Write a comment
#Remember to save your file
#Commit to main

#Git status shows you what you still need to commit and where your HEAD is. (head is the pointer that you use to see on which branch are you working on)

"
# git add [file]
is it useful to add to a pending stage the file you want to commit. You can easily do it from the git tab on Rstudio. You can choose
which file stage and then push.

# git commit -m '[message]'

code to commit your pending changes in your version history. They don't go into remote (therefore they are not shared).
It basically affects only your local repostiory.

# git push 

pushes the commits to remote which is basically the shared repository

"